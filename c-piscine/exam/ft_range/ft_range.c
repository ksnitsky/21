#include <unistd.h>
#include <stdlib.h>

int		*ft_range(int start, int end)
{
	int i;
	int *range;

	i = 0;
	range = malloc(sizeof(int) * (end - start));
	while (i <= end)
	{
		ran
	}
}

int		main()
{

	return (0);
}